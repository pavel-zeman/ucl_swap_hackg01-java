buildscript {
  repositories {
    maven { url gradlePluginsRepository }
  }

  dependencies {
    classpath("com.moowork.gradle:gradle-node-plugin:1.2.0")
  }
}

apply plugin: "com.moowork.node"

node {
  download = true
  version = '12.19.0'
}

/*task checkstyle(dependsOn: npmInstall, type: NpmTask, group: 'build') {
  args = ['run', 'eslint:teamcity']
}*/

task build(dependsOn: npmInstall, type: NpmTask, group: 'build') {
  args = ['run', 'package']
}

npmInstall.args = ['--loglevel', 'info']
